<?xml version="1.0" ?>
<project name="JarBuild" basedir="." default="jarfile">

	<target name="init" description="Initializes properties">
		<property name="project.name" value="helloWorld" />
		<property name="src.dir" value="src" />
		<property name="build.dir" value="build" />
		<property name="classes.dir" value="${build.dir}/classes" />
		<property name="mavenDependencies.dir"
			value="Maven Dependencies" />
	</target>

	<target name="prepare"
		description="Creates the build and classes directories" depends="init">
		<mkdir dir="${classes.dir}" />
	</target>

	<target name="compile" description="Compiles the code"
		depends="prepare">
		<javac includeantruntime="false" srcdir="${src.dir}"
			destdir="${classes.dir}">

		</javac>
		<dependency>
			<groupId>com.opencsv</groupId>
			<artifactId>opencsv</artifactId>
			<version>5.2</version>
		</dependency>
	</target>


	<target name="jarfile" description="JARs the code"
		depends="compile">
		<jar destfile="${build.dir}/${project.name}.jar"
			basedir="${classes.dir}" />
	</target>

	<target name="clean" description="Delete the build directory"
		depends="init">
		<delete dir="${build.dir}" />
	</target>

</project>